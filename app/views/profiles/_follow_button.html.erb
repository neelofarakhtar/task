<%= turbo_frame_tag(dom_id(@user)) do %>
    <% if following?(user) %>
        <%= button_to "Unfollow", profiles_unfollow_path(id: user.id), method: :delete %>
    <% else %>
        <%= button_to "Follow", profiles_follow_path(id: user.id), method: :post %>
    <% end %>
<% end %>